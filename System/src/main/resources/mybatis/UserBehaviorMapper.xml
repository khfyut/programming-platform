<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.programming.mapper.UserBehaviorMapper">

    <resultMap id="BaseResultMap" type="com.programming.entity.UserBehavior">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="behavior_type" property="behaviorType"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="details" property="details"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.programming.entity.UserBehavior" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_behavior (user_id, behavior_type, target_type, target_id, details, create_time)
        VALUES (#{userId}, #{behaviorType}, #{targetType}, #{targetId}, #{details}, NOW())
    </insert>

    <select id="findByUserIdAndType" resultMap="BaseResultMap">
        SELECT * FROM user_behavior 
        WHERE user_id = #{userId} 
        AND behavior_type = #{behaviorType}
        ORDER BY create_time DESC
    </select>

    <select id="findByUserIdAndTarget" resultMap="BaseResultMap">
        SELECT * FROM user_behavior 
        WHERE user_id = #{userId} 
        AND target_type = #{targetType} 
        AND target_id = #{targetId}
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <select id="countSubmitRecords" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM submit 
        WHERE user_id = #{userId} 
        AND problem_id = #{problemId}
    </select>

    <select id="getSubmitCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM submit 
        WHERE user_id = #{userId} 
        AND problem_id = #{problemId}
    </select>

</mapper>