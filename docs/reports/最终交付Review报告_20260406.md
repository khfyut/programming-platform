# 最终交付 Review 报告

生成时间：2026-04-06  
复审范围：前端代码、后端代码、构建与测试结果、数据库核查证据  
工作区：`D:\Desktop\毕业设计\Code`

## 1. 当前结论

基于 2026-04-06 这轮整改后的再次复核，上一版报告中的主要代码级交付阻断项已经完成修复：

1. 社区帖子和评论的越权修改、越权删除风险已补上资源归属校验。
2. Spring Security 不再全局 `permitAll`，已接入 JWT 认证过滤器并按路径收紧访问控制。
3. 个人主页推荐题的难度标签已兼容数值型难度，展示口径恢复正常。

当前结论调整为：**项目已具备进入最终签收准备的条件，但正式签收前仍建议补做一次真实数据库复核和一轮人工验收。**

## 2. 本轮实际验证结果

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 后端 `cmd /c mvn-local.cmd -q test` | 通过 | 含本轮新增社区权限单测 |
| 前端 `npm run build` | 通过 | 可正常产出构建包 |
| 前端 `npm run lint` | 通过 | `0 errors, 1959 warnings` |

补充说明：

- 本轮新增了社区权限相关单测，至少覆盖了“禁止修改他人帖子”和“禁止删除他人评论”等关键逻辑。
- 前端构建仍有大体积 chunk 警告，属于性能优化项，不构成当前签收阻断。

## 3. 本轮已关闭的问题

### 3.1 社区越权修改/删除

**修复位置**

- `System/src/main/java/com/programming/controller/CommunityController.java`
- `System/src/main/java/com/programming/service/CommunityService.java`
- `System/src/main/java/com/programming/service/impl/CommunityServiceImpl.java`
- `System/src/main/java/com/programming/mapper/CommunityMapper.java`
- `System/src/main/resources/mybatis/CommunityMapper.xml`

**修复结果**

- 更新和删除帖子/评论时，已显式传入当前登录用户 ID。
- 服务层先校验资源归属，再执行更新/删除。
- SQL 层追加了 `user_id` 条件，避免只靠业务层判断。
- 对未登录、资源不存在、操作他人内容等情况，已返回明确错误码。

### 3.2 Spring Security 访问控制

**修复位置**

- `System/src/main/java/com/programming/config/SecurityConfig.java`
- `System/src/main/java/com/programming/security/JwtAuthenticationFilter.java`

**修复结果**

- 新增 JWT 认证过滤器，把 token 解析接入 Spring Security 主链路。
- 公共接口、管理员接口和普通受保护接口已分层配置。
- 未登录请求会返回 `401`，越权管理员访问会返回 `403`。

### 3.3 推荐题难度标签

**修复位置**

- `frontend/src/views/UserProfile.vue`

**修复结果**

- 推荐题列表已在前端统一把数值型 `difficulty` 归一化为 `easy / medium / hard`。
- 难度标签样式不再因为数值类型而全部退化成默认分支。

## 4. 当前残留风险

### [R1] 数据库未完成 2026-04-06 当天的实时复核

本轮尝试重新连接本地 MySQL 实例时，仍未获得可用凭据，因此没法独立确认 2026-04-06 当天数据库真实状态。

当前可引用的最近一次成功数据库核查结果，仍然是 `docs/reports/数据库报告.md` 中 **2026-04-02** 的快照。也就是说：

- 可以认为“数据库曾在 2026-04-02 被成功核查”
- 不能把“数据库已于 2026-04-06 再次确认正常”作为已验证结论

### [R2] 自动化质量信号仍然偏弱

- 前端 `lint` 仍有 `1959` 条 warning
- 后端虽然已补最小单测，但整体自动化回归覆盖仍然有限

这不会阻止你进入签收准备，但会降低后续回归置信度。

## 5. 建议的最终签收步骤

建议按下面顺序完成最终签收：

1. 提供可用数据库连接方式，再执行一次真实数据库复核并更新时间戳。
2. 做一轮人工验收，重点覆盖：
   - 社区帖子本人可改、他人不可改
   - 社区评论本人可删、他人不可删
   - 未登录访问受保护接口返回 `401`
   - 普通用户访问管理员接口返回 `403`
   - 个人主页推荐题难度标签显示正确
3. 若上述检查通过，可进入最终签收。

## 6. 最终判断

截至 **2026-04-06**，本项目的主要代码级交付阻断项已基本清除。当前更准确的判断是：

**可以进入最终签收准备，但建议在完成一次真实数据库复核和人工验收后再正式签收。**
