<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.programming.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.programming.entity.Role">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="RoleWithPermissionsMap" type="com.programming.entity.Role" extends="BaseResultMap">
        <collection property="permissions" ofType="com.programming.entity.Permission">
            <id column="p_id" property="id"/>
            <result column="p_name" property="name"/>
            <result column="p_code" property="code"/>
            <result column="p_description" property="description"/>
        </collection>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM role ORDER BY id ASC
    </select>

    <select id="findById" resultMap="RoleWithPermissionsMap">
        SELECT r.*, p.id as p_id, p.name as p_name, p.code as p_code, p.description as p_description
        FROM role r
        LEFT JOIN role_permission rp ON r.id = rp.role_id
        LEFT JOIN permission p ON rp.permission_id = p.id
        WHERE r.id = #{id}
    </select>

    <insert id="insert" parameterType="com.programming.entity.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role (name, description, create_time, update_time)
        VALUES (#{name}, #{description}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.programming.entity.Role">
        UPDATE role 
        SET name = #{name}, description = #{description}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM role WHERE id = #{id}
    </delete>

    <delete id="deleteRolePermissions" parameterType="java.lang.Long">
        DELETE FROM role_permission WHERE role_id = #{roleId}
    </delete>

    <insert id="insertRolePermission">
        INSERT INTO role_permission (role_id, permission_id, create_time)
        VALUES (#{roleId}, #{permissionId}, NOW())
    </insert>

</mapper>
