<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.programming.mapper.AuditLogMapper">

    <resultMap id="AuditLogResultMap" type="com.programming.entity.AuditLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="operation" property="operation"/>
        <result column="resource_type" property="resourceType"/>
        <result column="resource_id" property="resourceId"/>
        <result column="detail" property="detail"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findPage" resultMap="AuditLogResultMap">
        SELECT * FROM audit_log 
        ORDER BY create_time DESC
        LIMIT #{size} OFFSET #{page}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM audit_log
    </select>

    <select id="findById" resultMap="AuditLogResultMap">
        SELECT * FROM audit_log WHERE id = #{id}
    </select>

    <select id="findByUserId" resultMap="AuditLogResultMap">
        SELECT * FROM audit_log 
        WHERE user_id = #{userId} 
        ORDER BY create_time DESC
        LIMIT #{size} OFFSET #{page}
    </select>

    <select id="findByResourceType" resultMap="AuditLogResultMap">
        SELECT * FROM audit_log 
        WHERE resource_type = #{resourceType} 
        ORDER BY create_time DESC
        LIMIT #{size} OFFSET #{page}
    </select>

    <insert id="insert" parameterType="com.programming.entity.AuditLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_log (user_id, operation, resource_type, resource_id, detail, ip_address, create_time)
        VALUES (#{userId}, #{operation}, #{resourceType}, #{resourceId}, #{detail}, #{ipAddress}, NOW())
    </insert>

    <delete id="deleteOldLogs">
        DELETE FROM audit_log WHERE create_time &lt; DATE_SUB(NOW(), INTERVAL #{days} DAY)
    </delete>

</mapper>
